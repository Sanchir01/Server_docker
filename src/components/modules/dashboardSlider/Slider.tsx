import { FC } from 'react'
import { GetAllCategoriesQuery } from '../../../../graphql/gql/graphql'

const Slider: FC<{items:GetAllCategoriesQuery}> = ({items}) => {
  return <div></div>
}

export default Slider